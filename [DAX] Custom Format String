VAR CurrentValue = SELECTEDMEASURE()
RETURN
    SWITCH (
        TRUE (),
        CurrentValue <= 1E3, "#,0.00",
        CurrentValue < 1E6, "#,0,. Ribu",
        CurrentValue < 1E9, "#,##0,,.00 Juta",
        CurrentValue <= 1E12, "#,0,,,.00 Milyar")
// For Percent use ▲ 0,00%;▼ 0,00%
